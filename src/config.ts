export const config = {
    debug: true,
    clientWidth: 1366,
    clientHeight: 854,

    // milliseconds for the key in the background to fade in
    keyAttack: 10,
    // milliseconds for the key in the background to fade out
    keyDecay: 390,
}